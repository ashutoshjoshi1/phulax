"""Phulax control plane API.

Hosted side of the trust boundary (ADR-0001): policy management, approvals,
and decision-event views. Receives metadata only — never tool credentials,
never raw content by default (ADR-0002).
"""

__version__ = "0.0.1"
