# apps/web — control plane dashboard (placeholder)

Not built yet, deliberately. Day 0 ships only Postgres + the smoke test
(see `00-day-zero.md`). The dashboard arrives in a later phase, after the
gateway produces real decision events worth displaying.

The marketing site lives in a separate repository (`Phulax-website`).
